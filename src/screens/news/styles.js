import { StyleSheet } from "react-native";
import { colorInterestCardBg } from "../../utils/colors";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colorInterestCardBg,
  },
});
